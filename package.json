{
  "name": "here-to-slay-dungeon-mapper",
  "version": "0.8.0",
  "private": true,
  "description": "A browser-first Here to Slay DUNGEONS mapper with an optional Tauri desktop shell.",
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'",
    "sync:version": "node scripts/sync-version.mjs",
    "prebuild:tauri-web": "npm run sync:version",
    "build:tauri-web": "node scripts/build-tauri-web.mjs",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.28.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.7.0",
    "@tauri-apps/plugin-window-state": "^2.4.1"
  }
}
